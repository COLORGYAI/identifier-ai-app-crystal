<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Colorgy AI Crystal ID</title>
<link rel="icon" type="image/png" href="favicon.png">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
<style>
:root {
  --white:#FFF9E8;
  --indigo:#0A0E2E;
  --gold:#c9a24d;
  --pink:#ff79b0;
  --shadow:0 12px 38px rgba(0,0,0,.3);
}

/* Reset */
*{margin:0;padding:0;box-sizing:border-box}
body{font-family:'Inter',sans-serif;background:var(--white);color:var(--indigo);line-height:1.6;}
a{text-decoration:none;color:inherit;scroll-behavior:smooth;}

/* Header */
header{
  background:var(--indigo);
  color:var(--white);
  padding:24px 32px;
  display:flex;
  justify-content:space-between;
  align-items:center;
  position:sticky;
  top:0;
  z-index:100;
}
header h1{
  font-size:3rem;
  font-weight:900;
  letter-spacing:.8px;
}
nav a{margin-left:24px;font-weight:600;color:white;}
nav a:hover{text-decoration:underline;}
header img{width:60px;height:60px;animation:spin 10s linear infinite;}
@keyframes spin{0%{transform:rotate(0deg);}100%{transform:rotate(360deg);}}

/* Hero */
.hero{
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  padding:120px 32px;
  background:linear-gradient(135deg,var(--indigo),var(--gold));
  color:white;
  text-align:center;
}
.hero h2{font-size:3.5rem;font-weight:900;margin-bottom:24px;line-height:1.1;}
.hero p{font-size:1.25rem;margin-bottom:36px;}
.btn{padding:16px 32px;border-radius:16px;font-weight:700;color:white;background:var(--pink);border:none;cursor:pointer;margin-right:12px;transition:all .25s;}
.btn:hover{transform:translateY(-2px);}
.btn-outline{background:transparent;color:white;border:2px solid var(--gold);}

/* Counters */
.counter{margin:20px 0;font-size:1.2rem;font-weight:700;}
.counter.highlight{color:var(--gold);animation:flash 1s infinite alternate;}
@keyframes flash{from{opacity:1;}to{opacity:.3;}}

/* Grid */
.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:32px;padding:40px 32px;}
.card{background:white;color:var(--indigo);padding:24px;border-radius:20px;box-shadow:var(--shadow);}
.card h3{font-size:1.5rem;margin-bottom:12px;}
.card .price{font-size:2rem;font-weight:800;color:var(--gold);margin-bottom:12px;}

/* Overlay */
.overlay{position:fixed;top:0;left:0;width:100%;height:100%;background:rgba(10,14,46,0.9);display:none;justify-content:center;align-items:center;z-index:200;}
.overlay.active{display:flex;}
.overlay-content{background:white;color:var(--indigo);padding:32px;border-radius:20px;max-width:500px;text-align:center;animation:fadeIn 0.5s ease;}
.overlay-content img{width:120px;height:120px;margin-bottom:16px;}
.overlay-content a{color:var(--pink);font-weight:700;}
.overlay-content button{margin-top:16px;padding:12px 24px;border:none;border-radius:12px;background:var(--gold);color:var(--indigo);cursor:pointer;}
@keyframes fadeIn{from{opacity:0;transform:scale(0.9);}to{opacity:1;transform:scale(1);}}

/* Footer */
footer{padding:32px;text-align:center;background:var(--indigo);color:white;}
footer a{color:var(--pink);}

/* Responsive */
@media(max-width:800px){.hero{flex-direction:column;align-items:center;text-align:center;}.hero h2{font-size:2.5rem;}}
</style>
</head>
<body>

<header>
  <h1>Colorgy ✦ AI Crystal ID</h1>
  <img src="ai-colorgy-crystal-identifier-app.2.pic.png" alt="Goldene Apfel mit Diamant">
  <nav>
    <a href="#home">Home</a>
    <a href="#features">Features</a>
    <a href="#pricing">Pricing</a>
    <a href="#about">About</a>
    <a href="#contact">Contact</a>
  </nav>
</header>

<!-- Hero -->
<section id="home" class="hero">
  <h2>Discover, Scan & Understand Crystals Instantly</h2>
  <p>Colorgy AI recognizes crystals using advanced vision, explains their energetic meaning, and lets you build your personal crystal profile.</p>
  <div class="counter" id="scanCounter">Scans today: 0 / 3</div>
  <div class="counter highlight" id="luckyCounter">Lucky 100th Pro: 0 / 100</div>
  <button class="btn" id="freeScanBtn">Start Free Scan</button>
  <a class="btn-outline" href="#pricing">View Plans</a>
</section>

<!-- Features -->
<section id="features" class="grid">
  <div class="card"><h3>Free Scan</h3><p>3 scans/day · 7 days free trial</p></div>
  <div class="card"><h3>Pro Intelligence</h3><p>Advanced energetic interpretation and guidance</p></div>
  <div class="card"><h3>Collector Profile</h3><p>Save, track and manage your personal crystal collection</p></div>
</section>

<!-- Pricing -->
<section id="pricing" class="grid">
  <div class="card">
    <h3>Free</h3>
    <div class="price">0$</div>
    <p>3 scans per day</p>
    <button onclick="window.location.href='https://buy.stripe.com/test_free'">Start Free</button>
  </div>
  <div class="card">
    <h3>Pro</h3>
    <div class="price">$4.99</div>
    <p>Unlimited scans + full interpretation</p>
    <button onclick="window.location.href='https://buy.stripe.com/cNi5kDcnw3aR6is3JT8Vi03
">Subscribe</button>
  </div>
</section>

<!-- About -->
<section id="about" class="grid">
  <div class="card">
    <h3>About Colorgy</h3>
    <p>AI powered crystal recognition and energetic interpretation system. Enhance your mind, body & spirit.</p>
  </div>
</section>

<!-- Contact -->
<section id="contact" class="grid">
  <div class="card">
    <h3>Contact</h3>
    <p>Email: support@colorgy.app</p>
  </div>
</section>

<!-- Overlay -->
<div class="overlay" id="overlay">
  <div class="overlay-content">
    <img src="https://images.unsplash.com/photo-1567225477277-c8162eb4991d?w=400&h=400&fit=crop" alt="Crystal">
    <h3 id="stoneName">Amethyst</h3>
    <p id="stoneMeta">Crown Chakra · Calming · Intuition</p>
    <p id="stoneDesc">Enhances clarity of mind, emotional balance, and spiritual awareness.</p>
    <a id="stoneLink" href="https://www.mineralienatlas.de/lexikon/index.php/Amethyst" target="_blank">Learn more</a>
    <button id="closeOverlay">Close</button>
  </div>
</div>

<footer>
  &copy; 2026 Colorgy AI Crystal ID | <a href="#">Impressum</a>
</footer>

<script>
let scansToday = 0;
let luckyCount = parseInt(localStorage.getItem("luckyCount") || "0");
const scanCounter = document.getElementById("scanCounter");
const luckyCounter = document.getElementById("luckyCounter");
const overlay = document.getElementById("overlay");
const freeScanBtn = document.getElementById("freeScanBtn");

function updateCounters(){
  scanCounter.textContent = `Scans today: ${scansToday} / 3`;
  luckyCounter.textContent = `Lucky 100th Pro: ${luckyCount % 100} / 100`;
}

freeScanBtn.addEventListener("click", () => {
  if(scansToday >= 3){
    window.location.href="https://buy.stripe.com/test_pro";
    return;
  }
  scansToday++;
  luckyCount++;
  localStorage.setItem("luckyCount", luckyCount);
  updateCounters();

  overlay.classList.add("active");
  document.getElementById("stoneName").textContent = "Amethyst";
  document.getElementById("stoneMeta").textContent = "Crown Chakra · Calming · Intuition";
  document.getElementById("stoneDesc").textContent = "Enhances clarity of mind, emotional balance, and spiritual awareness.";
  document.getElementById("stoneLink").href = "https://www.mineralienatlas.de/lexikon/index.php/Amethyst";

  if(luckyCount % 100 === 0){
    alert("🎉 Congrats! You are the 100th Pro user. 1 month free unlocked!");
  }
});

document.getElementById("closeOverlay").addEventListener("click", ()=>{
  overlay.classList.remove("active");
});

updateCounters();
</script>

</body>
</html>
